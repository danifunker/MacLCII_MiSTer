# Plan: Update MiSTer framework (`sys/`) from `../Template_MiSTer`

**Date:** 2026-06-04
**Branch:** `new-video-on-fix-egret`
**Template source:** `../Template_MiSTer` @ `f35083f` (2026-05-13, "Make HDMI_TX_INT available for HPS")

## Goal

Bring this core's MiSTer framework (`sys/`) up to the current upstream
template, while preserving the carefully-tuned core behavior (boot, video,
Egret, RAM mapping) and our local fixes.

## Hard constraint: verification is Quartus-only

The Verilator sim builds `verilator/sim.v` (`module emu`), **not**
`sys_top.v` / the framework. **No framework change can be validated in
Verilator.** The only validation path is a **Quartus rebuild on the Windows
machine** (`/Volumes/Temp/mistercore/MacLC_MiSTer`) followed by an on-hardware
boot. Plan accordingly: every framework step is "blind" until that rebuild.

---

## Phase 0 — Establish a known-good baseline (DO THIS FIRST)

The user explicitly wants the existing core tested before any framework change.

0.1 **Sim baseline (local).** Confirm current `master`/branch still boots:
```bash
cd verilator && make clean && make
./obj_dir/Vemu --screenshot 350 --stop-at-frame 351   # foreground
```
Verify `screenshot_frame_0350.png` shows the grey/black memory-test
checkerboard (not uniform grey). Also capture a later desktop frame
(~1490) per the SWIM-fix notes to confirm desktop still reached.

0.2 **FPGA baseline (Windows).** Keep the **current working `MacLC.rbf`** as
the golden reference. Note its build is already on disk:
`/Volumes/Temp/mistercore/MacLC_MiSTer/output_files/MacLC.rbf` (Jun 4 09:47).
Copy it aside as `MacLC_baseline_pre_fw.rbf`. Confirm it boots to desktop on
real hardware before we touch anything.

0.3 **Commit current state.** The 8MB RAM-size fix (`addrController_top.v`)
and the manual 5-bit `mode` fix (`sys_top.v`) are uncommitted. Commit them on
the branch first so the framework update is an isolated, revertible commit.

**Gate:** Do not start Phase 1 until 0.1–0.3 pass.

---

## Phase 1 — Inventory (already scouted; recorded here)

Files that differ (`sys/` ours vs template):

| File | Change | Risk |
|---|---|---|
| `osd.v` | 2 lines | trivial |
| `hps_io.sv` | 79 lines; **`HPS_BUS` width `[48:0]`→`[45:0]`**, new `F12KEYMOD` param | **HIGH (coupled interface)** |
| `sys_top.v` | 28 lines; audio boost, `HDMI_TX_INT`→`gp_in`, `N_BURST`, HPS cmds `0x2B/2F/3E/44`; reverts `.mode` to 4-bit | MEDIUM |
| `ascal.vhd` | full new version (~6k-line diff); `mode` still 5-bit | LOW (drop-in) |
| `audio_out.v` → `audio_out.sv` | renamed + new `.boost` port | MEDIUM (qip edits) |
| `emu_ports.vh` | **new** include file (emu port list) | optional |

**Compatibility confirmed:**
- The `emu` *instantiation* port list in template `sys_top.v` is **identical**
  to ours (no port renames).
- Our `MacLC.sv` emu port set already contains every port the new framework
  references (LED_*, SDRAM_*, UART_*, HDMI_*, VGA_*, FB_*).
- The **only** emu-interface change required is `HPS_BUS [48:0]→[45:0]`.

**Critical coupling:** `HPS_BUS` width must match in all three:
`hps_io.sv` (module decl), `sys_top.v` (instantiations), and
`MacLC.sv` (emu port). Template is internally consistent at `[45:0]`
(it dropped the `fb_en`+scanlines bits from the bus). Updating must be
all-or-nothing across these three files.

---

## Phase 2 — Execute the update

Do as **one commit**, in this order:

2.1 **Drop-in files** (no interface change):
- Copy `../Template_MiSTer/sys/ascal.vhd` → `sys/ascal.vhd`
- Copy `../Template_MiSTer/sys/osd.v` → `sys/osd.v`

2.2 **audio_out rename:**
- `git rm sys/audio_out.v`; copy `../Template_MiSTer/sys/audio_out.sv` → `sys/audio_out.sv`
- Edit `sys/sys.qip:24`: `VERILOG_FILE ... audio_out.v` →
  `SYSTEMVERILOG_FILE ... audio_out.sv` (template already has this form — can
  copy template `sys.qip`).
- Grep `files.qip` / `MacLC.qsf` for any other `audio_out.v` reference (none
  found in scout, but re-verify).

2.3 **hps_io.sv:**
- Copy `../Template_MiSTer/sys/hps_io.sv` → `sys/hps_io.sv` (brings `[45:0]`
  bus + `F12KEYMOD`).

2.4 **sys_top.v:**
- Copy `../Template_MiSTer/sys/sys_top.v` → `sys/sys_top.v`, **then re-apply
  our local fix**: change `.mode ({~lowlat,...})` →
  `.mode ({1'b0,~lowlat,...})` (keeps the ascal 5-bit `mode` Critical Warning
  cleared; upstream left it 4-bit).

2.5 **MacLC.sv (emu top) — the one interface edit:**
- `inout [48:0] HPS_BUS,` → `inout [45:0] HPS_BUS,`
- Decide on `emu_ports.vh`: **recommended NO** for this pass — keep our inline
  port list to minimize change surface. (Adopting the include is a separate,
  optional cleanup once the build is proven.)

2.6 **Quartus message-suppression:** revisit `MacLC.srf` — the new framework may
emit different IDs. Re-run and re-curate after first build.

---

## Phase 3 — Verify (Windows Quartus)

3.1 Sync the changed files to `/Volumes/Temp/mistercore/MacLC_MiSTer` (or pull
the branch there).

3.2 Full Quartus compile. Check:
- No errors (esp. **no port-width mismatch** on `HPS_BUS` — the canary that
  2.3/2.4/2.5 are consistent).
- Critical Warnings: the ascal `mode` one stays gone (our re-applied fix); the
  TG68K combinational loop is expected/inherent.
- Timing still passes (all slacks ≥ 0).

3.3 Deploy RBF, boot on hardware. Compare against `MacLC_baseline_pre_fw.rbf`:
desktop reached, video correct, Egret/ADB kbd+mouse alive, audio OK
(test the new boost path via OSD if exposed).

---

## Phase 4 — Rollback

All on a branch; the framework update is one isolated commit. If the Quartus
build or hardware boot regresses:
```bash
git revert <framework-commit>     # or git checkout master -- sys/ MacLC.sv
```
and redeploy `MacLC_baseline_pre_fw.rbf`.

---

## Open questions / decisions deferred

- **Audio boost UI:** the new `.boost` is driven by HPS `vol_boost`; confirm
  whether the OSD/conf-str needs a menu entry to actually expose it, or leave
  at default 0.
- **`emu_ports.vh` adoption:** defer to a follow-up cleanup once the raw update
  is proven on hardware.
- **10MB RAM validation** (separate track): the `addrController_top.v` 8MB fix
  still needs a MAME `-ramsize 10M` diff — unrelated to this framework work,
  but both should land before declaring the branch done.

---

## Status — COMPLETE (2026-06-04)

Executed on the Windows/Quartus machine (`C:\Temp\mistercore\MacLC_MiSTer`),
landed on branch `update-template`, pushed to origin:
- `bb493b1` — `.gitattributes` (scoped to the 7 touched files, churn-free) +
  `audio_out.v` removal
- `f8dd7d3` — framework sync to `Template_MiSTer @ f35083f`

Line-ending note: the repo has mixed committed EOLs (LF/CRLF/mixed). A scoped
`.gitattributes` pins only the touched files to `eol=lf`; edits were applied as
content deltas (not wholesale CRLF copies), so the diffs are content-only.

Per-phase outcome:
- **2.1–2.5 — done.** `ascal.vhd` was already current (no diff). `osd.v`
  (`OSD_HEADER`→`MENU_CORE`), `hps_io.sv` (`[48:0]`→`[45:0]` + `F12KEYMOD` +
  ioctl refactor), `sys_top.v` (`~HDMI_TX_INT`→gp_in, audio boost, `N_BURST`,
  HPS cmds 0x2B/2F/3E/44, fb_en/scanline bits off the bus), `audio_out.v`→
  `audio_out.sv`, `MacLC.sv` HPS_BUS. Local 5-bit `.mode` fix preserved.
- **2.6 — `MacLC.srf` reviewed; no change warranted.** Suppressed IDs target
  Qsys/Vip/PLL/port-width *IP* noise the swap didn't touch. The build's 3
  Critical Warnings are the known-inherent TG68K combinational loop (332081 ×2)
  and the SignalTap debug instance (35025) — both intentionally left visible.
- **3.2 — Quartus compile: 0 errors.** `HPS_BUS` 48→45 coupling held through
  elaboration; ascal `mode` Critical Warning stays cleared; timing closes
  (worst-case setup +0.592, hold +0.169).
- **3.3 — deployed + booted on hardware** (`192.168.99.143`, via
  `scripts/deploy_screenshot.sh`). Reaches the Mac "?"-disk / mouse-cursor
  stage with coherent video + a correctly-rendered cursor, and a responsive
  OSD (exercises the rewritten `hps_io.sv`). **This is the current boot ceiling
  — there is no full desktop yet**, so the desktop-gated checks are not
  verifiable here.

Deferred / out of scope for this update:
- Full-desktop boot, Egret/ADB kbd+mouse at desktop, audio + new `.boost` path
  — all gated on a bootable disk the core does not yet reach.
- `emu_ports.vh` adoption — keep inline port list (plan recommendation).
- Audio boost UI — left at default 0; no OSD menu entry added this pass.
- 10MB RAM validation — separate track, not part of this framework PR.

Rollback reference: `MacLC_baseline_pre_fw.rbf` (local, untracked).
